checkout:
  post:
    - rm -rf app
    - mkdir solutions
    - git clone "https://github.com/jupitervn/hasBrain_UserSignUpInfo.git" solutions
    - cd solutions
    - git checkout solution
    - cd ..
    - cp -r solutions/app app
    - ls -la
test:
  pre:
    - emulator -avd circleci-android22 -no-audio -no-window:
        background: true
        parallel: true
    - circle-android wait-for-boot
  override:
    - pwd
    - ./gradlew signup-uitest:connectedAndroidTest
  post:
    - cp -r signup-uitest/build/reports/androidTests/connected/* $CIRCLE_TEST_REPORTS
notify:
  webhooks:
   - url: http://54.255.201.98/hook/circle/complete-story